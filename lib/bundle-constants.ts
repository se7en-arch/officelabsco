// Pure constant, no server imports — safe to use from both client code
// (cart-store, BundlePopup) and server code (lib/bundle-check.ts and the
// API routes that use it).
export const BUNDLE_PROMO_CODE = 'BUNDLE10';
